#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a - first integer to be used.
 * @b -second integer to be used.
 *
 * Return: 0 success.
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
