#include "main.h"
/**
 * print_last_digit(int) - Function that prints the last digit of a number.
ZZ * @n: number whose last digit is printed.
 *
 * Return: 0 success.
 *
 */
int print_last_digit(int n)
{
	int last_digit = n % 10;
	int digit = last_digit / 10;

	_putchar(digit + '0');
}
