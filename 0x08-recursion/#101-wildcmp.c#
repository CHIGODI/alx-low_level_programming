#include "main.h"
/**
 * int wildcm - a function that compares two strings if identical
 * @s1: first string
 * @s2: second string
 *
 * Return: 1 if identical 0 otherwise
 */
int wildcmp(char *s1, char *s2)
{
	if (s1 == 0 && s2 == 0)
	{
		return (1);
	}
	if ( s1 == 0 && s2 == *)
	{
		return (1);
	}
	if (s1 == 0 || s2 == 0)
	{
		return (0);
	}
	if (*s1 == *s2 ||